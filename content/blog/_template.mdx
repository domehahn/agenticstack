---
title: "Your article title"
description: "A one-sentence summary shown in article lists, previews, and SEO metadata."
date: "2026-07-28"
author: "dome"
topics:
  - agentic-engineering
tags:
  - AI Agents
featured: false
draft: true
---

Write your article body here using Markdown and MDX.

## A section heading

Regular paragraphs, lists, tables, and links all work as standard Markdown.

```typescript
// Fenced code blocks get syntax highlighting, a copy button,
// and can carry a filename + highlighted lines:
const example = "like this";
```

<Note>
Use `<Note>`, `<Warning>`, `<Architecture>`, and `<KeyTakeaway>` for callouts.
</Note>

Set `draft: false` and give the file a real, URL-friendly filename (its
filename becomes the article's slug) when you're ready to publish it. See
`content/blog/README.md` for the full frontmatter reference.
